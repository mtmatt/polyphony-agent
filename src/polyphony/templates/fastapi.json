{
  "name": "FastAPI Project",
  "description": "Scaffold a new FastAPI project with tests and a basic endpoint.",
  "tasks": [
    {
      "id": "init_project",
      "description": "Initialize a new Python project and install fastapi and uvicorn.",
      "agent_type": "executor"
    },
    {
      "id": "create_main",
      "description": "Create a main.py file with a simple FastAPI app and a root endpoint.",
      "depends_on": ["init_project"],
      "agent_type": "executor"
    },
    {
      "id": "create_tests",
      "description": "Create a tests/test_main.py file using pytest and httpx to test the root endpoint.",
      "depends_on": ["create_main"],
      "agent_type": "executor"
    },
    {
      "id": "verify",
      "description": "Run the tests to ensure everything is working correctly.",
      "depends_on": ["create_tests"],
      "verification_command": "pytest",
      "agent_type": "executor"
    }
  ]
}
