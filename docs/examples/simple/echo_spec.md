# Echo Script Spec
Implement a Python script called `echo_cli.py` that takes a string as a command-line argument and prints it to the console in a stylized way using the `rich` library.

Requirements:
- Use `argparse` for CLI.
- Use `rich.panel.Panel` to wrap the output.
- Add a `--color` flag to specify the panel border color.
